[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "learning_ray_notes"
authors = [{name = "Jialin Yi", email = "jialinyi@sas.upenn.edu"}]
dynamic = ["0.1", "learning_ray_notes"]